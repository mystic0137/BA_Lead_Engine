
# uncomment if you are using make instead of docker image builds
# Installed separately in Dockerfile using CPU-only PyTorch wheels:
# pip install --extra-index-url https://download.pytorch.org/whl/cpu \
#     torch==2.7.0+cpu torchvision==0.22.0+cpu torchaudio==2.7.0+cpu

# this repo doesnt need GPU deps and installing GPU torch will bloat docker images.
# uncomment only if you explicitly implement GPU modules
# torch==2.7.0

fastapi==0.135.1
uvicorn==0.42.0
pydantic==2.12.5
pydantic-settings==2.14.1
python-multipart==0.0.29
pandas==2.3.3
numpy==2.2.6
scikit-learn==1.7.2
xgboost==3.2.0
onnx==1.20.1
onnxruntime==1.23.2
skl2onnx==1.20.0
onnxmltools==1.16.0
onnxconverter-common==1.16.0
httpx==0.28.1

# UI
streamlit==1.55.0
plotly==6.6.0
requests==2.32.3

# RAG / LLM
groq==0.30.0
chromadb==1.3.0
sentence-transformers==5.1.2
pypdf==6.1.3